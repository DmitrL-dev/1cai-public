# Руководство по работе с TODO маркерами

## 📝 Назначение

Этот документ определяет лучшие практики использования TODO, FIXME, HACK и подобных маркеров в кодовой базе.

---

## ✅ Хорошие примеры TODO

### Пример 1: Привязка к GitHub Issue

```python
# TODO(#1234): Реализовать ротацию OAuth2 refresh токенов
# Приоритет: P0, Оценка: 2 дня, Ответственный: @username
def refresh_token(token: str) -> str:
    pass
```

### Пример 2: С контекстом и приоритетом

```typescript
// TODO: Добавить rate limiting для защиты от злоупотреблений
// Приоритет: P1 (Высокий)
// Контекст: Сейчас нет защиты от DoS атак
// Оценка: 1 день
// Связано: результаты security audit
async function processRequest(req: Request): Promise<Response> {
  // ...
}
```

### Пример 3: Временное решение

```python
# HACK: Временное исправление для #5678, удалить после upstream fix
# Ожидаемое удаление: v6.0.0
# Upstream issue: https://github.com/external/repo/issues/123
if legacy_mode:
    return workaround_function()
```

---

## ❌ Плохие примеры TODO

### Пример 1: Без контекста

```python
# TODO: исправить это
def process_data():
    pass
```

**Проблема:** Нет информации о том, что нужно исправить и почему.

### Пример 2: Расплывчатое описание

```typescript
// TODO: улучшить производительность
function slowFunction() {
  // ...
}
```

**Проблема:** Нет конкретного действия, метрик, приоритета.

### Пример 3: Устаревший

```python
# TODO: Обновить до Python 2.7
# (Код уже на Python 3.11)
```

**Проблема:** Устарел, должен быть удалён.

---

## 🏷️ Типы маркеров

### TODO

**Использовать для:** Запланированных функций или улучшений

**Формат:**

```
TODO(#issue): Описание
Приоритет: P0/P1/P2
Оценка: X дней
Ответственный: @username (опционально)
```

**Пример:**

```python
# TODO(#1001): Добавить слой кэширования для embeddings
# Приоритет: P1, Оценка: 3 дня
# Ожидаемое улучшение: снижение latency на 50%
```

---

### FIXME

**Использовать для:** Известных багов, требующих исправления

**Формат:**

```
FIXME(#issue): Описание бага
Серьёзность: Critical/High/Medium/Low
Влияние: Описание влияния
Workaround: Временное решение (если есть)
```

**Пример:**

```typescript
// FIXME(#2002): Race condition при обновлении токена
// Серьёзность: High
// Влияние: Пользователи могут получать 401 ошибки периодически
// Workaround: Retry с exponential backoff
```

---

### HACK

**Использовать для:** Временных обходных решений

**Формат:**

```
HACK: Почему это временно
Причина: Почему правильное решение не реализовано
Удалить к: Версия или дата
Правильное решение: Что должно быть сделано вместо этого
```

**Пример:**

```python
# HACK: Hardcoded timeout для обхода бага в библиотеке
# Причина: Ожидание релиза библиотеки v2.0
# Удалить к: v6.0.0 (когда зависимость обновится)
# Правильное решение: Использовать встроенный timeout библиотеки
TIMEOUT = 30  # Должен быть конфигурируемым
```

---

### XXX

**Использовать для:** Предупреждений или важных заметок

**Формат:**

```
XXX: Предупреждение или важная заметка
Влияние: Что может пойти не так
```

**Пример:**

```typescript
// XXX: Эта функция вызывается из нескольких потоков
// Влияние: Должна быть потокобезопасной, не добавлять изменяемое состояние
function criticalFunction() {
  // ...
}
```

---

## 📊 Уровни приоритета

### P0 (Критичный)

- Блокирует production deployment
- Уязвимости безопасности
- Риски потери данных
- **Действие:** Создать GitHub Issue немедленно, назначить владельца

### P1 (Высокий)

- Важные функции
- Проблемы производительности
- Баги, видимые пользователям
- **Действие:** Создать GitHub Issue, запланировать на следующий спринт

### P2 (Низкий)

- Улучшения nice-to-have
- Очистка кода
- Обновления документации
- **Действие:** Добавить в backlog, решать когда есть время

---

## 🔄 Рабочий процесс

### 1. Перед добавлением TODO

**Спросите себя:**

1. Могу ли я исправить это сейчас? (Если да, исправьте вместо добавления TODO)
2. Есть ли уже GitHub Issue? (Если да, ссылайтесь на него)
3. Это действительно нужно? (Если нет, не добавляйте TODO)

### 2. Добавление TODO

```python
# TODO(#номер-issue): Чёткое описание
# Приоритет: P0/P1/P2
# Оценка: X дней
# Контекст: Почему это нужно
# Критерии приёмки: Что означает "готово"
```

### 3. Ревью TODO

**Еженедельно:**

- Проверить все P0 TODO (должно быть 0)
- Обновить P1 TODO с прогрессом
- Удалить устаревшие TODO

**Ежемесячно:**

- Конвертировать P1 TODO в GitHub Issues
- Архивировать или удалить P2 TODO

---

## 🚫 Антипаттерны

### 1. TODO вместо GitHub Issue

```python
# ❌ Плохо
# TODO: Реализовать весь OAuth2 flow с 10+ подзадачами

# ✅ Хорошо
# TODO(#1234): Реализовать OAuth2 flow (детали в issue)
```

### 2. Вечные TODO

```python
# ❌ Плохо (TODO с 2020 года, до сих пор не сделано)
# TODO: Отрефакторить это когда-нибудь

# ✅ Хорошо
# Либо исправьте сейчас, либо создайте Issue с дедлайном
```

### 3. Без приоритета

```python
# ❌ Плохо
# TODO: Добавить валидацию

# ✅ Хорошо
# TODO: Добавить валидацию email
# Приоритет: P1 (предотвращает невалидные данные в БД)
```

---

## 🛠️ Инструменты

### Pre-commit Hook

Предупреждает о новых TODO без контекста:

```bash
#!/bin/bash
# .git/hooks/pre-commit
NEW_TODOS=$(git diff --cached | grep -i "^+.*TODO" | grep -v "TODO(#" || true)
if [ -n "$NEW_TODOS" ]; then
    echo "⚠️  Предупреждение: TODO без ссылки на GitHub Issue"
    echo "$NEW_TODOS"
fi
```

### CI Check

Падает сборка при слишком большом количестве FIXME/HACK:

```yaml
# .github/workflows/code-quality.yml
- name: Проверка количества FIXME
  run: |
    COUNT=$(rg -i "FIXME|HACK" --type py --type ts -c | wc -l)
    if [ $COUNT -gt 10 ]; then
      echo "Слишком много FIXME/HACK: $COUNT"
      exit 1
    fi
```

### Интеграция с IDE

Расширение VS Code: **TODO Highlight**

- Подсвечивает TODO/FIXME/HACK
- Показывает в панели Problems
- Настраиваемые цвета

---

## 📈 Метрики

Отслеживать здоровье TODO:

- **Общее количество:** Должно уменьшаться со временем
- **Количество P0:** Всегда должно быть 0
- **Возраст:** Как долго существуют TODO (цель: <30 дней для P1)
- **Скорость решения:** TODO закрытых в неделю

---

## 🎯 Цели

| Метрика           | Текущее | Цель (3 месяца) |
| ----------------- | ------- | --------------- |
| Всего TODO        | 3,500+  | <1,000          |
| P0 TODO           | 5       | 0               |
| TODO с контекстом | ~20%    | >80%            |
| TODO → Issues     | 0%      | 50%             |

---

## 📚 Ссылки

- [Google Engineering Practices](https://google.github.io/eng-practices/)
- [TODO or not TODO](https://testing.googleblog.com/2017/10/code-health-to-comment-or-not-to-comment.html)
- [Technical Debt Management](https://martinfowler.com/bliki/TechnicalDebt.html)

---

**Последнее обновление:** 2025-11-22  
**Версия:** 1.0  
**Владелец:** Команда разработки
