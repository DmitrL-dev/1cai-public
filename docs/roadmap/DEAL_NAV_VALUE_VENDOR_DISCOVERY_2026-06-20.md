# Deal Nav Value/Vendor Discovery - 2026-06-20

## What Changed

- Sidebar `Deal` navigation now includes `Value Packs` and `Vendor`.
- Home fallback buyer cards now include `Value Packs` when Buyer Brief data is not loaded yet.
- Home fast entries now include `Value Packs` alongside demo, pilot, offer and engineering routes.

## Buyer Impact

The product no longer hides buyable packaging and vendor pre-sale audit behind indirect links. A buyer can see the path from pain to package to proposal without guessing where the commercial/product layer lives.

## Verification

- `npm run build` in `portal`
