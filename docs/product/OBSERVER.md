# Локальное наблюдение за выгрузкой

Проектирование и реализация в ветке dev8. Цель этапа — действующий цикл
проверка источника → захват при изменении → сохранённый отчёт с восстановлением.
Это часть O1; диагностика качества, lifecycle находок и runtime/business adapter
из O2 остаются обязательными следующими этапами.

Observer использует отдельный профиль SQLite, привязанный к проекту, путям
и текущему Windows principal. Только один обработчик удерживает файловую
блокировку профиля; после завершения процесса блокировку освобождает ОС.
Профиль размещают вне исходников. Он не является хранилищем секретов.

Проверка источников повторно использует защищённый WindowsSourceTree и два
прохода хеширования ядра, без копирования файлов и запуска scanner. При тех же
байтах и head новое построение/отчёт не нужны. Цена проверки — чтение выгрузки,
а не запрос LLM. Ограничения захвата ядра сохраняются. Изменения между опросами,
включая изменение с последующим возвратом, могут не попасть в снимок; атомарность
всей живой конфигурации во времени не доказана.

До захвата в журнал записываются UUID операции, ожидаемый head и последний
обработанный снимок. После сбоя используется тот же UUID: ядро сначала ищет
квитанцию. После подтверждения захвата сохраняется отдельная фаза отчёта;
ошибка отчёта не вызывает повторное построение графа. Конфликт head завершает
устаревшую попытку, следующий опрос выбирает актуальное состояние.

Отчёт показывает пару снимков, количество файловых изменений и первые 100
изменений с признаком неполного списка. Начальный отчёт помечен как baseline.
Отчёты сохраняются в одной транзакции с продвижением последнего обработанного
снимка. Качество и бизнес-метрики отмечаются как ещё не вычисленные.
Отчёты не отправляются наружу и не применяют изменений к конфигурации.

Три последовательные ошибки одной задачи останавливают её до явного retry.
Retry сохраняет UUID и фазу восстановления. Журнал ограничен 10 000 задачами;
автоматического удаления отчётов и orphan generations нет. В steady state
неизменные входы не создают новых задач. Отзыв прав останавливает worker и
запрещает выдачу сохранённых отчётов через CLI. Процессы того же SID и прямое
чтение файлов профиля не изолированы друг от друга.

Проверено: неизменные байты без scanner/новой задачи; изменение с прежними
размером и mtime; восстановление после подтверждённой публикации; ошибка отчёта
без повторного capture; внешний head; конкурирующий worker; отозванные права;
изменившаяся конфигурация слоёв; запуск установленного пакета в отдельном процессе.
Сценарий `scripts/verification/verify_observer_delivery.py` также завершает свой
бездействующий worker и проверяет освобождение блокировки новым процессом.
Авария между публикацией и записью отчёта моделируется в регрессионных тестах;
принудительное завершение процесса во время записи SQLite отдельно не проверено.

## Запуск

Установите отдельно собранный dev8, создайте проект ядра и соберите scanner.
Родитель каталога профиля должен существовать. Инициализация не перезаписывает
существующий профиль и не регистрирует задачу Windows автоматически.

```powershell
rentgen-observer init --registry C:\RentgenState\registry.sqlite3 --project PROJECT_UUID --profile C:\RentgenState\observer
rentgen-observer run --registry C:\RentgenState\registry.sqlite3 --project PROJECT_UUID --profile C:\RentgenState\observer --scanner C:\RentgenTools\bsl-scan.exe --interval 60
```

Worker работает в переднем плане, завершение — Ctrl+C (код 130). Для ограниченной
проверки добавьте `--cycles 2`; обычный запуск наблюдает до остановки. Интервал
5–86400 секунд отсчитывается после завершения цикла. Повторяющиеся события
`unchanged` и одинаковые ошибки не печатаются каждый раз; время последней
проверки остаётся в журнале. После незавершённого ошибочного ограниченного
запуска код выхода 2, после успешного — 0.

```powershell
rentgen-observer status --registry C:\RentgenState\registry.sqlite3 --project PROJECT_UUID --profile C:\RentgenState\observer --limit 1
rentgen-observer retry --registry C:\RentgenState\registry.sqlite3 --project PROJECT_UUID --profile C:\RentgenState\observer
```

`status` возвращает последние задачи и отчёты в JSON. Размер ответа ограничен
2 МиБ; уменьшите лимит, если ответ слишком велик. После исправления причины
ошибки `retry` разрешает продолжить прежнюю задачу, затем снова запустите worker.
Ошибка доступа к файлам или самому журналу завершает процесс сразу; предыдущая
запись восстановления остаётся. Отчёты и имена файлов могут быть чувствительными
данными: каталог профиля должен быть доступен только доверенным пользователям.
