# Продуктовый и архитектурный аудит — 12 сентября 2026

Аудит зафиксирован на текущем публичном срезе ветки `codex/edt-metadata-dev9` в
репозитории [DmitrL-dev/1cai-public](https://github.com/DmitrL-dev/1cai-public).
Проверялась ветка Windows x64 / Python 3.11 с core `0.1.0.dev8` и companion
`0.1.8`. Локальная проверка дала 403 успешных теста; Product CI для этого
среза проверяется после публикации. Это аудит текущей границы, а не заявление о
готовности всех пунктов дорожной карты.

## Что уже имеет хорошую основу

- Источник истины находится в локальном core: проект, права, снимки, черновики,
  MCP и квитанции не делегируют контроль модели.
- Операции привязаны к snapshot/commit, ожидаемой ревизии и хэшам; конфликт и
  потеря ответа переводятся в явный результат, а повторная запись не делается
  автоматически.
- Metadata preview строит два полных diff и связывает candidate с сохранённым
  бизнес- и платформенным доказательством. Preflight apply проверяет свежесть,
  права и живые байты, но запись пока запрещена.
- Observer хранит Git-находки, события и квитанции транзакционно; replay,
  resolve и reopen переживают перезапуск. Bounded watcher повторно проверяет
  HEAD перед публикацией.
- Общий metadata reader теперь разбирает проверенные корневые EDT `.mdo` и
  bounded `.form`; смешение EDT и Designer-кандидатов отклоняется fail-closed.
- Появился реальный CAS writer/undo для отдельной принадлежащей workspace-копии;
  live source и 1С остаются защищёнными от записи до native-квалификации.
- Публичная поставка собирается офлайн, имеет MIT-лицензию, фиксированные
  хэши и явные ограничения. Основная рыночная позиция описана в
  [COMPETITOR-MATRIX.md](COMPETITOR-MATRIX.md).

## Приоритетные слабые места

| Приоритет | Симптом и источник | Последствие | Исправление и критерий приёмки |
| --- | --- | --- | --- |
| P0 (частично закрыт) | `metadata_apply_gate.require_live_apply()` по-прежнему запрещает live apply; `metadata_workspace` меняет только явно принадлежащую копию и не имеет native EDT/1С записи | Рабочая конфигурация и база 1С ещё не обновляются напрямую | Подключить тот же intent → CAS → backup → atomic write → re-read → immutable receipt протокол к квалифицированному EDT/1С writer; принять на новой базе с UUID, формами, данными, чужой правкой и сбоем процесса |
| P0 | `GitWatcher.tick()` требует caller-supplied analyzer и внешний scheduler; в core нет daemon/уведомлений | Непрерывный аудит не запускается сам и не даёт владельцу отчёт без собственного оркестратора | Добавить bounded job scheduler, backoff и durable job lease; поставлять adapters BSL LS/Sonar/Vanessa/YAxUnit; анализировать только новый clean commit, учитывать ancestry/force-push и формировать отчёт с источниками |
| P1 | Обновление конфигураций и расширений не имеет three-way merge | Нельзя предсказуемо перенести доработки при обновлении типовой конфигурации | Ввести base/current/upstream модель с object/UUID/BSL conflict plan, dry-run, тестовой базой и rollback; начать с одной типовой конфигурации и форм/СКД |
| P1 | Observer фиксирует исходники и findings, но не получает runtime-данные | Отчёт владельцу не может достоверно показать деньги, простои или эффективность | Определить data adapters и schema метрик: источник, период, freshness, агрегация, пробелы и права; бизнес-значения выдавать только из подтверждённых данных |
| P1 | Платформа подтверждена на 8.3.27.2342 и собственной синтетической EDT-фикстуре | Нельзя объявлять совместимость с типовыми `.cf/.cfe`, старыми 8.3.x, расширениями и произвольными формами | Матрица версий/типов/объектов с отдельными evidence-пакетами; каждая неподдержанная комбинация должна отказываться до записи |
| P1 (частично закрыт) | Ранее `rentgen_core.metadata` пропускал EDT `.mdo` как opaque; после текущего среза поддержаны корневые `.mdo` и `.form` при явном `source_format="edt"` | Полный EDT inventory, owner/layer semantics, все типы объектов и матрица совместимости ещё не доказаны | Расширить ограниченный EDT inventory adapter каноническими UUID/owner/layer, отдельными лимитами и теми же SourceRef; добавить типовые проекты и negative cases |
| P1 | Spectorn ещё не входит в управляемый adapter | Нельзя обещать защищённый маршрут модели или доказать поведение при недоступности защиты | После получения инструкции Spectorn зафиксировать loopback/upstream маршрут, policy отказа, таймауты и отсутствие обхода; секреты не должны попадать в argv, логи и отчёты |
| P2 | `rentgen_core/cli.py` содержит около 1068 строк и совмещает parser, авторизацию и обработчики; `observer.py` около 504 строк совмещает capture, SQLite, lock и findings | Новая команда повышает риск побочных изменений и усложняет независимое ревью | Разделить parser/commands/use-cases, оставить тонкий dispatch; вынести SQLite repository и lock boundary из observer. Сначала сохранить публичные JSON-контракты и добавить contract tests |

## Архитектурные решения

Граница ядра выбрана правильно для открытого продукта: модель является источником
предложения, а не полномочий; платформа 1С, BSL и тесты дают отдельные receipts.
SQLite и файловые хэши подходят для локального self-hosted профиля, если размеры,
срок хранения и восстановление после сбоя остаются явными.

Главный риск — преждевременно назвать orchestration API автономным агентом.
Сейчас watcher — синхронный адаптер одного прохода, а metadata gate — read-only
квалификация. В документации и README это должно оставаться видимым до появления
реального writer, scheduler и analyzer adapters. Любой новый внешний процесс
должен получать project/snapshot binding и проходить повторную авторизацию перед
публикацией результата.

## Выбранное направление

Рентген позиционируется как полностью открытый MIT/self-hosted control plane
проверяемой разработки 1С. Существующие IDE, BSL LS, SonarQube, Vanessa, EDT MCP
и модели подключаются адаптерами. Собственная ценность — evidence-first lifecycle:
изменение, проверка, сохранение доработок при обновлении, постоянный аудит и
отчёт владельцу с источником каждой метрики. Это направление не требует
закрывать экосистему и позволяет подключить Spectorn как защищённый маршрут,
когда его локальный контракт будет принят.

## Порядок следующей работы

1. Провести native business-data test для metadata rename и затем writer/undo на
   рабочей копии с намеренно прерванным процессом.
2. Поставить scheduler и первый read-only analyzer adapter для Git watcher;
   сохранить durable job state и уведомления после доказательства идемпотентности.
3. Начать three-way update на одной типовой конфигурации и зафиксировать
   матрицу совместимости, включая формы, СКД и расширения.
4. Подключить Spectorn по предоставленной инструкции и добавить negative-path
   acceptance без обхода защиты.
5. Только после этих доказательств поднимать версию и публиковать следующий
   prerelease; текущие dev8/0.1.8 теги остаются неизменными.

Статусы и границы приёмки синхронизированы с [READINESS.md](READINESS.md) и
[ROADMAP.md](../../ROADMAP.md).
