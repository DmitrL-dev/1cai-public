// Собственный синтетический тест Рентгена; MIT.
Процедура ИсполняемыеСценарии() Экспорт
    ЮТТесты.ДобавитьТест("ОшибкаЗаписиПередается");
КонецПроцедуры

Процедура ОшибкаЗаписиПередается() Экспорт
    ОшибкаПолучена = Ложь;
    Попытка
        RentgenPlatformProbe.СохранитьДокумент(Неопределено);
    Исключение
        ОшибкаПолучена = Истина;
    КонецПопытки;
    ЮТест.ОжидаетЧто(ОшибкаПолучена).Равно(Истина);
КонецПроцедуры
