# OBSOLETE / DO NOT RUN.
# This was a one-shot migration that moved src/services/*.ts into the
# now-deleted `frontend-portal/` frontend. That frontend has been removed
# (superseded by `portal/`), so running this script would recreate a dead
# directory and destructively move real backend files. Kept only as history.
Write-Host "refactor_frontend.ps1 is obsolete (frontend-portal was removed). No action taken." -ForegroundColor Yellow
exit 0
